import type { Metadata } from "next"

export const metadata: Metadata = {
    title: "Body Fat Calculator",
    description: "Estimate your body fat percentage with the free SetWise calculator. Use proven formulas to track your body composition progress.",
}
